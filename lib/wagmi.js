import { createConfig, http } from "wagmi";
import { celo, celolfajores  fom "wagmi/chain";
export cont wagmiConfi = createConfig({
  chains: [celoAlfajores, celo],
  transports: 
    [celoAlfajores.id]: htp()
    [celo.id]: http(
  },
  ssr: true
});